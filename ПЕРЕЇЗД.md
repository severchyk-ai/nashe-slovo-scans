# Переїзд проєкту на іншу машину

Складено 16.09.2026, виправлено того ж дня після першого переїзду.

Проєкт майже не прив'язаний до комп'ютера. Жорсткими лишилися тільки теки
даних (`NS_MASTERS`, `NS_WORK`, `NS_PDF`) і шлях до Tesseract — усі вгорі
`ns-lib.ps1`, у блоці «налаштування». Решту скрипти знаходять самі:
Ghostscript, NAPS2 і Python — за файлом, `tessdata` — поруч зі скриптами,
інші `ns-*.ps1` — через `$PSScriptRoot`.

⚠︎ Спершу тут стояло «усі шляхи в рядках 6-8 і 16-17, в інших скриптах
жорстких шляхів немає». Це було **неправдою**: NAPS2 і `tessdata` були вписані
під ім'я користувача `sever`, а `ns-rebuild-frame-2000.ps1` викликав решту
скриптів за повним шляхом. Подробиці — у `CLAUDE.md`, розділ «Переїзд
16.09.2026».

## 1. Що переносити

| що | розмір | обов'язково |
|---|---|---|
| `C:\NS_MASTERS` | 39 ГБ | **ТАК — це єдине незамінне** |
| `C:\Users\sever\Documents\Scans` (скрипти, CLAUDE.md, tessdata) | 5,2 ГБ | так |
| `C:\NS_PDF` | 1,1 ГБ | ні, але копіювати швидше, ніж перезбирати 9 годин |
| `C:\NS_WORK` | — | **ні**, це сміття; варті уваги лише `_zrazky` і `_pdf_before_*` |

## 2. Копіювання

Через зовнішній диск, `robocopy` — він зберігає атрибути, зокрема
**лише для читання** на майстрах:

    robocopy C:\NS_MASTERS E:\NS_MASTERS /MIR /COPY:DAT /DCOPY:DAT /R:2 /W:2 /MT:8
    robocopy "C:\Users\sever\Documents\Scans" E:\Scans /MIR /COPY:DAT /R:2 /W:2
    robocopy C:\NS_PDF E:\NS_PDF /MIR /COPY:DAT /R:2 /W:2

⚠︎ **Майстри не класти в OneDrive.** Синхронізація конфліктує з атрибутом
лише-для-читання, а Files On-Demand може перетворити файли на заглушки.

## 3. Що встановити на новій машині

Версії, на яких усе перевірено:

| інструмент | версія |
|---|---|
| ImageMagick | 7.1.2-29 Q16 x64 |
| Tesseract | 5.4.0 |
| Python | 3.14.3 |
| ocrmypdf / pikepdf / img2pdf | 17.11.0 / 10.13.0 / 0.6.3 |
| Ghostscript | 10.07.1 |
| poppler (pdftoppm) | 25.07.0 |
| NAPS2 | той самий, що й був |

Мовні файли Tesseract (`ukr`, `pol`, `osd`, `eng`) лежать у `Scans\tessdata`
і переїжджають разом зі скриптами — окремо завантажувати не треба.

## 4. Профіль сканера

Скопіювати `%AppData%\NAPS2\profiles.xml` і `config.xml` — профіль `Arhiv400`
переїде цілим. Драйвер сканера встановити ДО цього, інакше профіль не побачить
пристрій і його доведеться перевибрати.

⚠︎ **Профіль краще не набирати руками.** У збереженому `profiles.xml` стоїть
**PageSize = A3**, 400 dpi, 24-bit Color, Brightness/Contrast 0/0, автоматична
обрізка. У старому описі проєкту помилково значилося «Custom 295 x 415» —
майстри показують 297,0-301,9 x 413,6-420,1 мм, тобто A3 з обрізкою по вмісту.
Тому копіювати файл, а не відтворювати параметри.

## 5. Якщо шляхи будуть інші

Правити лише `ns-lib.ps1`, блок «налаштування» вгорі:

    $script:NS_MASTERS = "D:\NS_MASTERS"
    $script:NS_WORK    = "D:\NS_WORK"
    $script:NS_PDF     = "D:\NS_PDF"
    $script:TESSERACT  = "C:\Program Files\Tesseract-OCR\tesseract.exe"

`TESSDATA` правити не треба: він береться з теки самих скриптів. Теку
`Scans` можна класти куди завгодно, аби `tessdata` лежала поруч.

Найпростіше — покласти все за тими самими шляхами, тоді не міняти нічого.

## 6. Перевірка після переїзду — ОБОВ'ЯЗКОВА

    cd <тека зі скриптами>
    .\ns-verify.ps1        # звіряє SHA-256 кожної сторінки з маніфестом

Має бути **0 розбіжностей, 0 відсутніх**.

⚠︎ Якщо перевірка скаржиться, що сторінки не лише-для-читання (копіювання
могло загубити атрибут), повернути його:

    Get-ChildItem C:\NS_MASTERS -Recurse -Filter *.tif | ForEach-Object { $_.IsReadOnly = $true }

Атрибут не косметичний: правило проєкту вважає сторінку надійно прийнятою лише
тоді, коли файл на місці, лише-для-читання і збігається за хешем.

## 7. Наостанок

- Перевірити вільне місце: зараз 45 ГБ, але повний проєкт (1956-2013) виросте
  приблизно до 400 ГБ плюс стільки ж на резерв.
- Ярлик «Наше слово» на робочому столі **створюється автоматично** —
  `НАЛАШТУВАННЯ.ps1`, крок 8, викликає `ns-shortcut.ps1` щоразу заново, щоб
  ярлик вів на `ns-menu.ps1` саме в новій теці `Scans`. У режимі `-Check` крок
  перевіряє, що ярлик є і веде туди, куди треба. Якщо ярлик колись загубиться:
  `powershell -NoProfile -ExecutionPolicy Bypass -File .\ns-shortcut.ps1`.
  (16.09.2026 цей пункт був пам'яткою, і ярлик на новій машині забули.)
- Пробний прогін: `.\ns-issue.ps1 -Seq 2227 -Force` — має зібратися без
  помилок; майстри при цьому звіряються до і після.

## 8. Еталон пробного прогону: 2227 на старому лептопі

| | старий лептоп | допуск |
|---|---|---|
| сторінок | 10 | **точно** |
| розмір сторінки | 869,52 × 1197,6 pt | **точно** — це геометрія, від версій не залежить |
| розмір файлу | 19 772 222 байти (18,9 МБ) | кілька відсотків |
| OCR | 15 161 слово, 101 підозріле (0,67 %) | кілька десятків слів |
| шапка | підтверджує номер 2227 | точно |
| відтінок паперу B−R | 3,0 | близько |
| зауважень QC | немає | точно |
| версії | ImageMagick 7.1.2-29, Tesseract 5.4.0, ocrmypdf 17.11.0 | — |

Інша кількість сторінок, інший розмір сторінки або різниця в **сотні** слів —
привід зупинитися й розібратися, а не продовжувати. Слова рахувати за
сайдкаром `_ocr.txt`, **не** через `pdftotext` (див. `CLAUDE.md`).

**Звіряти не вручну, а скриптом** — еталон записано в `ns-reference.json`:

    .\ns-issue.ps1 -Seq 2227 -Force
    .\ns-compare.ps1

`ns-compare` рахує слова тим самим кодом, що й `ns-qc`, і виводить таблицю
«еталон / зараз / допуск». Код виходу 0 — збіг, 1 — є розбіжності.

Старий лептоп не вимикати й нічого з нього не видаляти, доки нова машина не
пройде `ns-verify` і цей прогін: до того він лишається оригіналом.
