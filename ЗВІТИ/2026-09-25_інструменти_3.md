# Звіт відділу інструментів, 25.09.2026 (3): багатодисковий «кінець дня», місце на дисках, фаза 3 (огляд)

Усе — git-коміти в `Scans`: `d8fb0dd`, `74079e7`, `b71c292`.

## 1. ns-endday: усі диски за `_scope.json`

- Проходить ВСІ підключені диски з текою `NS_BACKUP`; на кожному береже лише те, що в його `NS_BACKUP\_scope.json`
  (`{"years":[…],"jobs":[…]}`). Диск без `_scope.json` **не чіпається**, лише називається в звіті з підказкою, як його завести.
- `D:\NS_BACKUP\_scope.json` записано: роки 2000, 2001; частини masters, pdf, scans, archive, naps2 (з дозволу головної).
  `F:` має scope: рік 2002; masters, pdf.
- Повна звірка SHA-256 раз на 7 днів окремо на кожному диску (`повна звірка [F:\]: ok` у `endday.log`).
- Підсумок «скільки сторінок має копію» по роках: копією вважається файл у `<диск>\NS_BACKUP\NS_MASTERS\<рік>\<номер>\<файл>`
  з довжиною з маніфесту на диску, чий scope охоплює рік. 25.09: 2000 — 536/536, 2001 — 510/510, 2002 — 550/550, **без копії 0**.
  Для року без диска (2003) щодня буде «БЕЗ КОПІЇ N» червоним.
- git push перевірено: коміт `d8fb0dd` на `origin`.

## 2. Місце на дисках — виміри (12:40)

| | вільно | що треба |
|---|---|---|
| F: (FAT32) | 2,528 ГБ | PDF 2002: 3 шт. = 0,064 ГБ (16,3; 21,6; 29,0 МБ), ~52 решти × 19,8 МБ ≈ 1,0-1,1 ГБ → влазить, лишається ≈1,4 ГБ; майстри 2002 на F: 29,378 ГБ, дописувати нічого |
| D: (exFAT) | 0,028 ГБ | лишився 1 PDF (2278, ≈19 МБ); прогін ендею дописав ≈0,55 ГБ; решта PDF 2001 після огляду не влізе |

Мій попередній рядок «нові номери 2002 майже не влізуть на F:» був оцінкою, а не виміром, — хибним. Головна поправила.

`ns-backup` (коміт `b71c292`):
- місце рахується **до** копіювання по частинах (masters, naps2, scans, archive, pdf); запас 2 % + 1 МБ (≤ 32 МБ), для PDF — 4 МБ;
- **майстри** — цілком або жодного: «МАЙСТРИ НЕ ВЛІЗУТЬ…» червоним, код 1; мовчки не пропускаються;
- **PDF** — по одному через `.part` + перевірка розміру + перейменування, скільки влізе; решта — «PDF ПРОПУЩЕНО за браком місця» червоним, код 3; scans/archive/naps2 — так само;
- прогноз «місця вистачить ще на ~N PDF (середній X МБ)»; `-DryRun`; тест-змінна `NS_TEST_FREE` (лише для проб).
Проби: F: DryRun — 130 PDF; D: DryRun — 1 PDF; удаваний вільний обсяг 1 ГБ проти 29,38 ГБ майстрів → код 1; 50 МБ для 3 PDF → 2 скопійовано, 1 пропущено, код 3, `.part` немає, час файлів збережено.

## 3. Фаза 3 конвеєра: огляд

- `ns-review.ps1`: черга номерів у стані `review`/`fix`; для кожного — PDF без OCR (відкривається), список позначених сторінок з `prepare.json`
  (нагляд краю, `Review` ниток, дірки біля друку, не зведене до розміру, нерівна рамка), нотатки. **A** — `ready`; **N** + текст — `fix`
  (нотатка в `review_notes` маніфесту, запис у `state_log`). Режими без запитів: `-Status`, `-List`, `-Seq N -Accept`, `-Seq N -Note "…"`,
  `-Fixes` (нотатки для Claude), `-Seq N -Fixed "що поправлено"` (`fix` → `review`).
- `ns-prepare` наприкінці ставить стан: `ready`, якщо позначок 0 І є паспорт року (`_catalog\years\<рік>.json`), інакше `review`.
  Паспортів ще немає — тому всі йдуть на огляд.
- Меню: рядок «Конвеєр: …» у шапці, пункт «Огляд», «Зібрати PDF» не бере номерів у `review`/`fix`.
- Проби (пісочниця з копіями маніфестів): review → fix → review → ready; нотатки, `state_log`, реєстр — збігаються; інтерактив A/N перевірено через вхідний потік.
- Стан `review` проставлено 2320–2327 (2320–2325 і 2326, 2327 зібрані до появи цього коду). Зараз: `review 8`.

## 4. Пакет 2326–2331 (`ns-prepare-batch.ps1`, 2 одночасно)

| номер | час | нитки | найдальша | зрізи корінця, мм | великі дірки | рамка |
|---|---|---|---|---|---|---|
| 2326 | 14,8 хв | 77 | 5,7 мм | 3-6,5 | 20/20 | усі рівні |
| 2327 | 15,1 хв | 63 | 5,1 мм | 3-6 | 20/20 | усі рівні |

| 2328 | 13,5 хв | 31 | 4,2 мм | 3-5 | 20/20 | усі рівні; ⚠ стор. 1, 9, 10 без ниток (перевірено й на майстрах: лише зшивач; на 10 смуга скла 1,8 мм) — зріз з медіани номера 4,0 мм, Review: оператору глянути ці три |
| 2329 | 13,7 хв | 118 | 5,1 мм | 3,5-6 | 20/20 | усі рівні |
| 2330 | 13,1 хв | 120 | 5,8 мм | 4-6,5 | 20/20 | усі рівні |
| 2331 | 13,2 хв | 122 | 5,5 мм | 3,5-6,5 | 20/20 | усі рівні |

Пакет 2326–2331: 42 хв, збійних 0, НАГЛЯД (edgecheck) 0, не зведених до розміру 0. Розкид найдальшої нитки в номері 2,0-3,2 мм; по 60 сторінках зрізи мін 3, мед 4, макс 6,5 мм.
Стан `review`: 2320–2331 (12 номерів чекають огляду оператора). Не більше двох партій наперед: 2332–2337 — лише після огляду.

## 5. Розподіл дисків (за вказівкою головної, 25.09)

`_scope.json` (розділення частин за роками — новий ключ `years_by_job`, `ns-backup -JobYears "pdf=2001,2002"`, коміт `10fb7a8`):
- **D:** роки 2000, 2001; masters, scans, archive, naps2 (без pdf; 14 PDF 2001, що вже там, не чіпаю). Вільно 0,027 ГБ.
- **F:** майстри 2002; PDF 2001 і 2002. Після запуску на F: PDF 2001 — 19 шт., PDF 2002 — 3 шт., `.part` немає; вільно 2,176 ГБ; місця ще на ~112 PDF (середній 19,7 МБ). Повна звірка SHA-256: D: 1046 стор., F: 550 стор. — усі збігаються.
- Ендей 25.09: git коміт `d8fb0dd`, push `ok (origin)`; резерв D: ok, F: ok; без копії 0.
- Оцінка на все: PDF 2001+2002 усіх ≈ 2,0 ГБ із 2,53 → лишається ≈0,5 ГБ на F:.

ЗАПАМ'ЯТАТИ: (1) `ns-prepare-batch.ps1 -Seq 2332..2337` — партія по два одночасно, ≈7 хв/номер при двох паралельно; ns-prepare сам ставить стан `review`. (2) Скрипти, що правляться Python-скриптом: перед заміною `re.sub(r'+
','
',s)` (були подвійні ``), запис `utf-8-sig`, `newline='
'`; шаблони зі слешами — лише через файл-патч, не heredoc. (3) У PowerShell не називати допоміжну функцію `R`/`Rv` (аліаси `Invoke-History`/`Remove-Variable`). (4) `NS_TEST_FREE` (байти) в `ns-backup` вдає вільне місце — лише для проб. (5) Наступне за планом: перший номер 2003 → замір зшивання й рядок `NS_SPINE_RULES`, паспорт року `_catalog\years3.json`; фаза 4 конвеєра (`ns-conveyor -Night`) — лише після слова оператора щодо Планувальника; перебудова 2267/2318/2319 з новою рамкою — після слова оператора через головну.

ПОТРІБНА ЗМІНА СКРИПТУ: немає.
ЗАПАМ'ЯТАТИ: мій `ns-prepare.ps1` мав у файлі подвійні `\r` (`\r\r\n`) — виправлено; правити такі файли Python з нормалізацією `re.sub(r'\r+\n','\n',s)`, інакше Edit не знаходить рядка.
