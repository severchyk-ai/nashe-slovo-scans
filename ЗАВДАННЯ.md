# Розпорядження по відділах

Пише сюди головна сесія. Відділ бере своє завдання, виконує, ставить позначку
й кладе звіт у `ЗВІТИ\` (ім'я файлу: `РРРР-ММ-ДД_<відділ>.md`).
Позначки: `[ ]` чекає, `[~]` в роботі, `[x]` зроблено (з датою).

## Відділ сканування

- [x] 24.09.2026 — 2002 рік відскановано повністю: 52 номери (2318-2369),
      551 стор., `ns-verify` 0 розбіжностей. Звіт `ЗВІТИ\2026-09-24_сканування.md`.
- [ ] Сканувати **2003 рік** — номер, дату й № у році брати з газети.
      Перший номер 2003 — сказати інструментам (замір зшивання).
      **З 25.09 після Q `ns-scan` сам запускає приймання** (`ns-accept`): аркуш
      підвалів усіх сторінок у вікні перегляду; Enter — прийнято, номер сторінки —
      перезняти. На першому номері 2003 — записати у звіт, як воно пройшло
      (перший живий прогін, гілку «перезняти» ще ніхто не пробував).
- [ ] Вкладку («Світанок» тощо) позначати клавішею **V** одразу, щойно дійшли.
- [ ] Після кожних ~10 номерів — `ns-verify.ps1` **і**
      `ns-pagecheck.ps1 -Year Y -Band -BandMm 22 -PerSheet 2` (порядок і дублі
      ловить лише око за підвалами: 2350, 2365, 2369); результат рядком у звіт.
- [ ] Перш ніж прибрати дубль — порівняти якість обох копій і прибрати ГІРШУ
      (2369 стор. 7: перекіс −1,34° проти −0,45°).

## Відділ збирання номерів

- [x] 24.09.2026 — 2318 зібрано: 10 стор., 16,1 МБ, 7,8 хв, 8366 слів,
      підозрілих 1,10 %, QC 1 (стор. 9 B−R — вміст). Звіт
      `ЗВІТИ\2026-09-24_збирання.md`. ⚠︎ edgescan стор. 6 (верх) очима не дивились.
      Завдання було: **2318 — ЗБИРАТИ** (оператор схвалив 24.09). Prep і render у
      `C:\NS_WORK\2318` готові за схваленою версією: `ns-build.ps1 -Seq 2318`,
      далі `ns-qc.ps1 -Seq 2318` і `python ns-edgescan.py`. Звіт числами.
- [x] 24.09.2026 — 2319 зібрано: 10 стор., 20,6 МБ, ~10 хв, 14 243 слова,
      підозрілих 0,65 %, QC 0, edgescan 0. Звіт `ЗВІТИ\2026-09-24_збирання_2.md`.
- [x] 25.09.2026 — 2267, 2318, 2319 перебудовано з новою рамкою; 2323 зібрано.
      Звіт `ЗВІТИ\2026-09-25_збирання.md`. 2267: 18,65 МБ, 1,15 %, QC 0;
      2318: 16,35 МБ, 1,06 %, QC 1 (стор. 9 B−R, як раніше); 2319: 21,61 МБ,
      0,59 %, QC 0; 2323: 29,1 МБ (16 стор.), 0,67 %, QC 1 (стор. 5 — 92 слова,
      вкладка). Нерівних рамок 17 → 4 (+2 у 2323, 0,5-0,9 мм). Старі PDF —
      `C:\NS_WORK\_old_pdf_25.09` (прибрати після слова оператора).
      ⚠︎ Очима не дивились: 2267 стор. 3 верх (edgescan 85 %, нове), 2323 стор. 5.
      Перебудова наявного номера: `ns-render -Seq N`, тоді `ns-build -Seq N -Force`.
- [x] 25.09.2026 — пакет 2268-2272 зібрано: по 10 стор., 19,1-20,3 МБ,
      підозрілих 0,60-1,09 %, QC 0, edgescan 0, framecheck 0, «НАГЛЯД» 0;
      ~1,1 год. Звіт `ЗВІТИ\2026-09-25_збирання_2.md`. Чекає огляду оператора
      (глянути: 2268 стор. 2 верх, стор. 10 низ).
- [ ] **НАСТУПНЕ. 2001 рік, два пакети: 2273-2277, тоді 2278-2282.** Той самий
      шлях і звіт (по рядку головній після кожного пакета). 2280 перезнято
      цілком 17.09 — звичайний номер. Якщо оператор знайде ваду в 2268-2272 —
      зупинитися й чекати розпорядження.
- [x] (виконано вище) **2001 рік пакетом по 5: 2268, 2269, 2270, 2271, 2272.**
      Номери 2001 року не мають проколів корінця й не потребують інструментів —
      звичайний шлях `ns-prep` → `ns-render` → `ns-build` → `ns-qc` → `ns-edgescan`
      → `ns-framecheck`. Сторони, які `ns-edgecheck` позначає `<< НАГЛЯД`, — у звіт
      списком (номер/стор./бік); `page_edge` руками НЕ писати. Після пакета —
      рядок головній і звіт числами; оператор дивиться PDF пакетом.
      Нагадування: 2270 — дубль стор. 5 видалено давно; 2294 і 2308 уже зібрані.
- [ ] Хвости 2002 для збирання (зі звіту сканування): **2323** — `insert_pages`
      немає; **2332, 2339, 2348** — записати кути вкладки (`ns-rotcheck`, тоді
      очима); **2355** — обкладинка першою, поворот 270°, порядок — очима;
      **2335** — порядок сторінок не перевірено (немає підвалу); **2363** стор.
      9-10, **2369** стор. 7-8 — чорні кути, краї очима.
- [ ] 2002 рік — лише ті номери, які головна передасть ПІСЛЯ інструментів
      (замір ниток, `page_edge`, `fill_holes`) і схвалення оператора. Самому
      2002 рік не брати. Звіт числами: сторінки, розмір PDF, % підозрілих, QC, час.
- [ ] Для кожного нового номера з вкладкою чи календарем — спершу глянути
      краї (`ns-edgecheck.ps1 -Seq N -Sheet`), а вже тоді збирати.

## Відділ інструментів

- [x] 25.09.2026 — 2323 доріз за оглядом оператора (звіт, «Додаток 6»);
      оператор: «без зауважень». Лишається p8 `8L 1,6` → «2323 готовий»
      головній і збиранню.
- [x] 25.09.2026 — **КОНВЕЄР, фаза 1** — коміт `b012b5e`: `ns-endday.ps1`
      (git commit/push; резерв на диск із текою `NS_BACKUP`, без диска — лише
      git; повна звірка SHA-256 раз на 7 днів; журнал `endday.log`),
      `ns-backup -Quick`, пункт меню «Завершити день». Випробувано на піску
      `C:\NS_WORK\_sandbox`. **Push не перевірено** — немає remote (чекає
      приватного сховища GitHub від оператора).
- [x] 25.09.2026 — **КОНВЕЄР, фаза 2** — коміт `cd6a2e4`, звіт
      `ЗВІТИ\2026-09-25_інструменти.md`: `ns-accept.ps1` + `ns-acceptcheck.py`
      (кадри, розмір, порожні, дублі кожної з кожною, аркуш підвалів; Enter —
      прийнято, номер — перезняти, Q — не зараз), `ns-scan` викликає приймання
      після Q (`-NoAccept` вимикає); поле `state` + `state_log` у маніфесті,
      колонка `state` у реєстрі; меню «Прийняти номер N». Реальні 2340, 2350,
      2354, 2355, 2369 — 0 зауважень, 4-8 с на номер. НЕ перевірено: push, диск,
      живий скан із вікном, гілка «перезняти».
- [ ] **НАСТУПНЕ (25.09). 2002 рік пакетом: 2320, 2321, 2322, 2324, 2325** —
      `ns-spinescan.py` → `page_edge` посторінково → `-FillHoles` → PDF без OCR
      (п'ять файлів разом) оператору на огляд. Це зараз вузьке місце: збирання
      2002 стоїть, доки ти не підготуєш номери. Шукай, що можна автоматизувати
      на пакеті (однакова глибина в межах номера? частка дірок, що заростає?) —
      числами у звіт. Фаза 3 конвеєра (меню «Конвеєр»/«Огляд») — ПІСЛЯ пакета.
- [ ] Коли з'явиться перший номер 2003 — поміряти зшивання й додати рядок у
      `NS_SPINE_RULES` (prep з `-NoEdgeClean` + `ns-edgecheck -Sheet`, `ns-spinescan`).
- [ ] **Кожен номер 2002 р. далі:** `ns-spinescan.py` → `page_edge`
      посторінково, `-FillHoles` → PDF без OCR оператору → після схвалення
      повідомити головну.
- [x] 25.09.2026 — дослідні клони (~2,3 ГБ) і .v1/.v2 прибрано; `ns-penscan`,
      `ns-ocrprobe` видаляють лише свою mkdtemp-теку (звіт, «Додаток 5»).
      `C:\NS_WORK\2319_before_pen.pdf` видалено 25.09 за словом оператора.

- [x] 24.09.2026 — проколи 2318 і 2319 (схвалено оператором); зміни скриптів
      сканування переглянуто — конфліктів немає; червона ручка в OCR
      (`ns-penmask.py`, 2319 перезібрано: стор. 10 785 → 837 слів, зображення
      байт у байт ті самі); A/B рамки (нерівних 30 → 5). Звіт
      `ЗВІТИ\2026-09-24_інструменти_2.md`, «Додаток 4».
## Спільне

- [~] **Резерв на флешки (25.09, інструменти, ПЕРЕД пакетом 2002):** D: (KINGSTON
      57,7 ГБ, NS_BACKUP 21.09, 2000+2001, вільно 0,6 ГБ) — лише звірити;
      F: (32 ГБ, порожня) — майстри 2002 (29 ГБ). Потрібен ключ вибору років
      у `ns-backup`. З D: нічого не видаляти.
- [ ] **Резервна копія повністю**: зовнішній диск (~1 ТБ; проєкт ~485 ГБ
      майстрів). Щойно з'явиться — `ns-backup.ps1 -Dest <диск>` і звірка SHA-256.
- [x] 25.09.2026 — git remote: `origin` = `github.com/severchyk-ai/nashe-slovo-scans`
      (приватне), перший push `aff05d8`. «Завершити день» тепер робить push.

## Зроблено

- [x] 24.09.2026 — CLAUDE.md розділено: правила (72 КБ) і `ІСТОРІЯ.md` (61 КБ).
- [x] 24.09.2026 — заведено `СТАН.md`, `ЗАВДАННЯ.md`, `ВІДДІЛИ.md`, теку `ЗВІТИ\`.
- [x] 24.09.2026 — у таблицю скриптів додано `ns-insert`, `ns-droppage`,
      `ns-livecheck`, `ns-rotcheck`, `ns-backup`, `ns-inpaint.py`; виправлено
      застарілу вказівку запускати через `.bat` (їх на цій машині немає).
