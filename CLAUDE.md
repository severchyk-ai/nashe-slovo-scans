# ЯКЩО ЦЕ ПЕРШИЙ ЗАПУСК НА НОВІЙ МАШИНІ — ПРОЧИТАЙ ЦЕ

Проєкт щойно переїхав з іншого комп'ютера. Порядок дій:

1. Прочитай `ПРИВІТАННЯ_ДЛЯ_CLAUDE.md` — там стан справ, правила і мої помилки,
   яких не варто повторювати.
2. Перевір, чи все встановлено: у теці проєкту на флешці є `НАЛАШТУВАННЯ.ps1`.
   Запуск `.\НАЛАШТУВАННЯ.ps1 -Check` нічого не міняє, лише показує, чого бракує.
3. Якщо чогось бракує — той самий сценарій без `-Check` встановить і розкладе.
4. Після цього обов'язково `.\ns-verify.ps1` — має бути 0 розбіжностей.
   **Сторінок має бути 736** (536 за 2000 рік + 20 номерів 2001 × 10), а TIF-файлів
   у `NS_MASTERS` — 737. Зайвий — `_catalog\removed\2240_2000-07-02_p11.tif`,
   див. «Відкладений кадр 2240» нижче. Зниклої сторінки не шукати.
5. Перше діло по суті: **резервна копія майстрів**. Їх 39 ГБ в одному примірнику,
   і це найбільший ризик проєкту.

Коли все стоїть і перевірка чиста — цей блок більше не потрібен, читай далі.

✓ **16.09.2026 нова машина пройшла обидві перевірки.** `ns-verify`: 736 сторінок,
0 розбіжностей. Пробна збірка 2227 збіглася з еталоном (`ПЕРЕЇЗД.md`, розд. 8):
10 стор., 869,52 × 1197,6 pt точно, 19 769 903 байти (−0,01 %), 15 148 слів
(−13), B−R 3,0, зауважень QC немає, 9,0 хв. ImageMagick тут **7.1.2-31 Q16-HDRI**
(на старому був -29 Q16) — на результат не вплинуло.
⚠︎ `-Check` писав «ImageMagick / poppler БРАКУЄ» — хибно: обидва стоять і в PATH,
але процес Claude Code запущено до встановлення. **Лікується перезапуском
застосунку**, а не підкладанням `$env:Path` у команди. Повторна звірка —
`ns-compare.ps1`.

---

## Хто редагує робочі файли (рішення оператора, 24.09.2026)

**Редагує лише ГОЛОВНА сесія**: `CLAUDE.md`, `СТАН.md`, `ЗАВДАННЯ.md`,
`ВІДДІЛИ.md`, `ІСТОРІЯ.md`. Так у проєкті лишається один зв'язний опис, а не
чотири розбіжні. Відділи туди НЕ пишуть — вони звітують, а головна вносить.

**Хто що змінює** (головна, за дорученням оператора, 24.09.2026). Кожен файл
має ОДНОГО господаря; двоє в одному файлі — конфлікт, який ніхто не помітить.

| Що | Господар | Як |
|---|---|---|
| `CLAUDE.md`, `СТАН.md`, `ЗАВДАННЯ.md`, `ВІДДІЛИ.md`, `ІСТОРІЯ.md` | головна | за звітами відділів |
| пам'ять Claude (`~\.claude\projects\…Scans\memory\`) — спільна для ВСІХ сесій | головна | відділ пише в звіті «ЗАПАМ'ЯТАТИ: …» |
| `ЗВІТИ\РРРР-ММ-ДД_<відділ>[_N].md` | свій відділ | чужих звітів не чіпати |
| скрипти: `ns-*.ps1`, `*.py`, `ns-lib.ps1` (з `NS_SPINE_RULES`), `ns-reference.json` | **інструменти** | див. нижче |
| майстри `NS_MASTERS` (скан, `ns-rescan`, `ns-droppage`, `ns-renumber`, `ns-insert`) | сканування | лише скриптами, з відома оператора |
| маніфест номера — поля обробки (`page_edge`, `page_rotate`, `page_order`, `fill_holes`) | відділ, що ТРИМАЄ номер | лише скриптами, не руками |
| `NS_WORK\<номер>`, `NS_PDF` | відділ, що ТРИМАЄ номер | — |
| реєстр `issues.csv`, суми, решта маніфесту | самі скрипти | руками не правити |

- **Номер тримає один відділ за раз.** Хто — записано в `ЗАВДАННЯ.md`. Зараз
  номер 2002 р. іде: інструменти (замір, зріз, заростання) → оператор
  схвалює PDF без OCR → головна передає збиранню. Передає лише головна.
- **Скрипти змінює лише відділ інструментів.** Інший відділ пише в звіті рядок
  «ПОТРІБНА ЗМІНА СКРИПТУ: файл — що не так — число/приклад», головна ставить
  завдання інструментам. Інструменти перед зміною роблять копію
  `<файл>.before-<суть>`, після — перевіряють BOM і пишуть у звіт суть зміни
  й вимір, що її виправдовує.
  **Виняток:** скрипт зупинив сканування, і оператор у сесії сканування ПРЯМО
  велить виправити — тоді можна, з тією самою копією `.before-`, і в звіті
  рядок «ПРАВКА СКРИПТУ ЗА ВКАЗІВКОЮ ОПЕРАТОРА: …»; інструменти її переглядають.
- **Скрипт-перевірка видаляє ЛИШЕ те, що сам створив** — у власній тимчасовій
  теці, ніколи не «свої» файли за шаблоном імені в робочій теці номера.
  (24.09: `ns-penscan.py` видалив `NS_WORK\2319\render\p01-p10.jpg`; відновлено
  з prep, байт у байт.) Дослідні клони номерів — лише з позначкою, прибирати.
- **Помилку в правилах** відділ не виправляє сам — рядок «ПОМИЛКА В ПРАВИЛАХ: …».
- **Виміри в `ІСТОРІЯ.md` переносить головна ДОСЛІВНО**, числами з рядка звіту,
  не переказуючи своїми словами. Переказане число — це вже не вимір; половина
  помилок цього проєкту почалася з того, що хтось переказав замість поміряти.
- Сесія «Наше Слово» (розробка до 24.09) заархівована 24.09 — лише як архів
  розмов; прав не має. Відкривати заново не треба.

Якщо відділ бачить помилку в `CLAUDE.md` — він не виправляє її сам, а пише про
неї в звіті окремим рядком «ПОМИЛКА В ПРАВИЛАХ: …». Так головна побачить її
одразу, а не загубить у тексті.

## Файли проєкту: хто що читає

| Файл | Коли читати |
|---|---|
| **`CLAUDE.md`** | завжди — це правила й чинний стан |
| **`СТАН.md`** | на початку будь-якої сесії: де проєкт зараз (переписується, не дописується) |
| **`ЗАВДАННЯ.md`** | розпорядження по відділах; сесія бере своє |
| **`ЗВІТИ\`** | звіти відділів числами; головна сесія читає нові |
| **`ВІДДІЛИ.md`** | як відкривати сесії-відділи, які моделі й стартові рядки |
| **`ІСТОРІЯ.md`** | лише на вимогу: чому правила саме такі, що вже пробували |

Поділ на відділи (24.09.2026, рішення оператора): сканування, збирання номерів,
інструменти, і коротка головна сесія, що читає стан і роздає розпорядження.
Причина — вартість: довга сесія з переглядом зображень з'їдає ліміт за день.

## Де історія рішень

Щоб цей файл лишався робочим, усе «чому саме так» — виміри, відкинуті підходи,
розбори помилок — переїхало у **`ІСТОРІЯ.md`** (24.09.2026). Тут лишилися самі
правила й чинний стан. Перш ніж пропонувати «очевидне» покращення, пошукати
його в історії: майже все вже пробували, і майже все виміряно.

# Оцифрування архіву «Наше слово» (річники 2000–2013)

Обсяг: ~715 номерів, ~7300 сторінок. Перший номер 2000 р. — **2214** (підтверджено
з першої сторінки, не розрахунком). Останній 2013 р. — приблизно 2930.

⚠︎ **Проєкт зараз перебудовується** під розгорнуту специфікацію: реєстр номерів,
маніфести, відновлюваність після падіння, автоматичний QC. План і поточна фаза —
у `C:\Users\sever\.claude\plans\optimized-rolling-ocean.md`. Скрипти, описані
нижче, — це робочий набір Фази 2 (ручний пілот); їх замінить набір `ns-*`.

## Фізичний процес

Річник **розшито**, сторінки скануються поодинці, притиснуті кришкою сканера
з чорним тлом.

Це не дрібниця, а розв'язання головної проблеми проєкту. Раніше, зі скріпленого
річника, зворот сторінки **просвічувався** так, що текст з іншого боку читався
в полях. Витрачено багато часу на програмні спроби це прибрати — усі провалилися
за однією причиною: просвітлення й текстура паперу на цьому масштабі математично
один тип сигналу (локальне відхилення від згладженого фону), тому будь-який
фільтр, що прибирає одне, нищить друге. Виміряні втрати текстури: драйверний
`Bleed Through Prevention` — 72 %, flat-field корекція в постобробці — 40 %.

Чорна кришка над окремим аркушем усуває просвітлення **фізично, без жодної втрати
текстури**. Перевірено: під сильним підсиленням контрасту в білих полях видно
лише зерно паперу, жодних дзеркальних букв.

## Профіль сканування

NAPS2, профіль `Arhiv400`:

| Параметр | Значення |
|---|---|
| Роздільність | 400 dpi |
| Режим | 24-bit Color, усі сторінки без винятку |
| Область | **298 × 418 мм** (з 17.09.2026, 10:59; до того 16.09 19:33 — 298 × 415) — `PageSize=Custom` у profiles.xml |
| Cropping (Paper tab, драйвер) | **Fixed to Transport** (з 16.09.2026) |
| **Color Matching** | **None** |
| Brightness / Contrast | **0 / 0** |
| Maximum quality | увімкнено |
| Deskew, Apply brightness/contrast after scan | вимкнені |

⚠︎ **Виправлено 16.09.2026.** Тут довго стояло «Custom 295 × 415 мм» — це було
неправильно. У збереженому `profiles.xml` профіль `Arhiv400` має `PageSize=A3`,
а `CustomPageSize` порожній. Перевірено за самими майстрами: 2214 має рівно
4677 × 6615 px = **297,0 × 420,1 мм**, тобто A3 без залишку. Пізніші номери
дають 297,0-301,9 × 413,6-420,1 мм — це вже працює автоматична обрізка, і вона
подекуди захоплює **ширше за A3**, бо ріже по вмісту, а не по заданому розміру.

⚠︎ **Змінено 16.09.2026: автоматична обрізка → стала область 298 × 415.**
«Automatic Multiple» різала аркуш на кілька кадрів (2266 стор. 8, 2267 стор. 4 —
частину заголовка втрачено), а на сторінці з чорною смугою по краю зрізала
~15 мм смуги, бо не відрізняла її від чорної кришки. Стала область цього не
робить. **Розмір задає профіль NAPS2, а не вікно драйвера:** з драйвером
300 × 415 і профілем A3 скан виходив рівно A3 (4677 × 6615). Тому 298 × 415
записано в `profiles.xml` (`<PageSize>Custom</PageSize>` + `<CustomPageSize>`
у мм); копія попереднього — `profiles.xml.before-298x415` поруч. Перевірка:
сторінка має бути **4693 × 6583 px**. Номер 2287 знято впереміш: стор. 1–6
ще A3, з 7-ї — 298 × 415.

⚠︎ **17.09.2026: 415 мм замало, стало 418.** Замір крайніх 0,5 мм кадру
(кришка < ~110, папір ~180+): при 298 × 415 знизу на КОЖНОМУ скані папір
обривався на краю кадру (217-219) — аркуші 2001 р. мають 417-418,5 мм, і
на 2280/1 не вмістилися ~3,5 мм плашки з орнаментом. При A3 (297 × 420) навпаки
зрізався правий край (161-210). Ширина 298 достатня. Після 298 × 418 на 2280/1
усі чотири краї 98-138, тобто край аркуша лежить на межі кадру — впритул.
Знято з 415 і зрізані знизу: 2268/10, 2287 стор. 7-10; з A3 зрізані справа:
2287 стор. 1-6. Лівий край на всіх сталих сканах — папір до краю кадру:
аркуш притиснуто до напрямної, розміром області це не лікується.
**Рішення оператора 17.09.2026: 298 × 418 достатньо.** На 2280 стор. 3-4 замір
показує папір до нижнього краю кадру (аркуш 418,2 мм + ~1,5 мм тіні згори),
тобто низ поміщається впритул або без ~1 мм чистого поля; оператор перевірив
аркуші й підтвердив, що нічого не втрачено. Не пропонувати більшу область знову
без нового доказу втрати вмісту.
Номер 2280 перезнято цілком 17.09 (усі 10 стор., старі майстри в `removed`,
звірено попарно — ті самі аркуші в тому самому порядку).

**Для відтворення профілю:** не набирати параметри руками за цим описом, а
копіювати `%AppData%\NAPS2\profiles.xml` — тоді профіль переїде таким, яким
він справді працював.

### Чому Color Matching: None

Перебрано всі режими й заміряно відсікання світлих тонів по гістограмі:

| Режим | Пікселів на одному рівні |
|---|---|
| Document | 63–83 % |
| Photo | 42,7 % |
| Custom 1 | 42,9 % |
| Mix | 65,7 % |
| **None** | **~5 %, плавний розподіл** |

Усі «іменовані» пресети — це ICC-профілі, які пакують колірну корекцію **разом
із тоновою кривою**; крива й дає спайк. `None` вимикає ICC повністю і лишає
лінійний сигнал сенсора. Ціна — відтінок синього логотипу зсунутий приблизно на
3°, що виправляється в постобробці (див. нижче).

⚠︎ **Brightness/Contrast −50/−100, які тут стояли раніше, були помилкою.** Вони не
прибирали відсікання, а лише масштабували вже відсічені дані з 0…255 у 6…237.
Перевірка «чи є пікселі на 255» проходила, хоча текстуру паперу було знищено.
Будь-яке відхилення Contrast від нуля (перевірено ±15, ±20) псує і текстуру,
і підсилює просвітлення. Не чіпати.

### Виміряні характеристики

| | |
|---|---|
| Майстер | 65–68 МБ/сторінка |
| Увесь проєкт | ~485 ГБ майстрів (+ стільки ж на резерв) |
| OCR | ~88 с/сторінка → ~15 хв на номер, ~178 год на проєкт |

88 с/сторінку означає, що OCR має йти нічним пакетом, а не разом зі скануванням.

## Інструменти

| | Розташування / примітка |
|---|---|
| ImageMagick (`magick`) | у PATH |
| Tesseract | `C:\Program Files\Tesseract-OCR\tesseract.exe` — **не в PATH**, викликати повним шляхом |
| tessdata (ukr, pol, eng, osd) | тека `tessdata` поруч зі скриптами (`Join-Path $PSScriptRoot "tessdata"`) — типова тека Tesseract має лише eng+osd |
| Python | справжній `python.exe`, знайдений за відповіддю на `--version`; `ns-lib.ps1` ставить його теку першою в PATH процесу |
| NAPS2 | `NAPS2.Console.exe`, шукається в Program Files, Program Files (x86), WindowsApps, тоді `Get-Command` |
| ocrmypdf 17.11 + pikepdf | викликати як `python -m ocrmypdf` (`ocrmypdf.exe` не в PATH) |
| Ghostscript 10.07.1 | `C:\Program Files\gs\gs10.07.1\bin\gswin64c.exe` |

⚠︎ **Скрипти `.ps1` з кирилицею мають бути в UTF-8 З BOM.** PowerShell 5.1 без
BOM читає `.ps1` як ANSI, кирилиця в коментарях і рядках калічиться, і файл
перестає розбиратися взагалі («Missing closing '}'»). Симптом впізнати легко:
у помилці видно `Ð‘Ð ÐÐšÐ£Ð„` замість тексту. Перевірка: перші три байти файлу
мають бути `EF BB BF`.

**Обгортка для ocrmypdf обов'язкова** — перед викликом задати:
`TESSDATA_PREFIX` на проєктну теку tessdata і додати теку Tesseract у PATH,
інакше він не знайде ані мов, ані самого tesseract.

`--optimize 3` зі специфікації вимагає ще й `pngquant`, якого немає. Не ставимо:
pngquant стискає PNG, а в нас кольорові JPEG, і рівень 1 уже дає 93,5 % економії
(65 МБ → 3,4 МБ).

## Структура каталогу

```
C:\NS_MASTERS\                             майстри, read-only, ніколи не змінюються
   _catalog\
      issues.csv                           головний реєстр номерів
      checksums\2214.sha256 ...
      logs\
   2000\
      2214_2000-01-02\
         2214_2000-01-02_p01.tif ... _p10.tif
         _manifest.json                    метадані + стан + хеш кожної сторінки
         _scan.log
C:\NS_WORK\                                похідні, можна видаляти й перебудовувати
C:\NS_PDF\2000\2214.pdf                    доставка; ім'я = наскрізний номер
```

Шляхи задані вгорі `ns-lib.ps1` — переїзд на зовнішній диск це зміна трьох
рядків. **Не в OneDrive**: синхронізація ~394 ГБ і ризик «зневоднення» майстрів
до хмарних заглушок; плюс кирилиця в шляху вже ламала `pdftoppm`.

В архів Об'єднання кладеться в теку `Nasze Słowo - rocznik <рік>`, **ім'я файлу =
наскрізний номер** (той, що надрукований у дужках на першій сторінці).

## Скрипти

**Нове покоління (`ns-*`), працює з каталогом:**

| Скрипт | Що робить |
|---|---|
| `ns-lib.ps1` | Спільна бібліотека: шляхи, реєстр, маніфести, хеші, обрізка. Підключається іншими. |
| **`ns-menu.ps1`** | **Головна точка входу.** Ярлик «Наше слово» на робочому столі. Показує стан каталогу, пропонує наступний номер, викликає решту. |
| **`ns-scan.ps1 -Seq N -Date РРРР-ММ-ДД -NoInYear K`** | Сканує номер **і одразу** реєструє кожну сторінку. Без `-Date` — продовжує перерваний. Клавіші в циклі: **Enter** — сканувати, **R** — перезняти останню сторінку, **V** — позначити вкладку зі своєю нумерацією, **Q** — завершити. Показ сторінки — за 0,8 с, перевірки йдуть окремим процесом. |
| **`ns-verify.ps1 [-Seq N]`** | Звірка всіх хешів + перевірки QC 1–4 (кількість сторінок, дублікати, розриви). Спершу перебудовує реєстр із маніфестів. |
| **`ns-issue.ps1 -Seq N` / `-All`** | Увесь шлях від майстрів до PDF однією командою: 3 → 3b → 5–6. Після успіху прибирає похідні. |
| `ns-prep.ps1 -Seq N` | Етап 3, лише геометрія: обрізка смуги, автоповорот (OSD), перекос. Звіряє хеші майстрів **до і після**. |
| `ns-render.ps1 -Seq N` | Етап 3b: баланс білого (один на номер), 300 dpi, зрізання брудного краю, зведення сторінок до спільного розміру, біла рамка 7 мм, JPEG q55. |
| `ns-build.ps1 -Seq N [-PageOrder …]` | Етапи 5–6: img2pdf → ocrmypdf (ukr+pol, PDF/A-2b) → `NS_PDF`. |
| `ns-qc.ps1 -Seq N` / `-All` | Етап 7, перевірки 5–8: порожня сторінка, якість OCR, звірка номера з шапки, відхилення ширини. |
| **`ns-compare.ps1 [-Seq N] [-Save]`** | Звірка зібраного номера з еталоном у `ns-reference.json` (за умовчанням 2227): сторінки, розмір сторінки, байти, слова OCR, відтінок, QC. Після переїзду чи оновлення інструментів. Нічого не збирає. |
| **`ns-rescan.ps1 -Seq N -Page P [-FromFile f] -Reason "…"`** | Пересканувати (або взяти готовий файл) і замінити одну сторінку. Старий майстер — у `_catalog\removed`, у маніфесті поле `replaced`. Перевіряє кадри й розмір, **не вміст** — що на склі той аркуш, підтверджує оператор. Прибирає застарілі похідні номера. |
| **`ns-scancheck.ps1 -Seq N [-Compare "a,b"] [-Extra "f1;f2"]`** | Якість майстрів проти сусідніх номерів: шпиль тонової кривої (ознака Color Matching), вибиті світла/тіні, папір, B−R. Після зміни драйвера чи машини. |
| **`python ns-edgescan.py <тека або PDF>`** | Детектор залишених смуг краю в готових PDF (40 dpi, мінімум вглиб, пропускає рамку). Контроль на штучних сторінках, без нього висновку не дає. Автор — сесія старого лептопа. Позначене дивитися очима: рамка календаря чи плашка теж дають «смугу». |
| **`python ns-spinescan.py`** | Замір проколів від нитки на корінці: скільки й наскільки вглиб (2319: 150 шт., найдальша 6,9 мм від краю prep). Дає посторінковий зріз для `page_edge`; міряти КОЖЕН номер 2002 р. — сталої глибини немає. Автор — відділ інструментів; параметри див. у самому скрипті. |
| **`ns-penmask.py`** | Маска перекреслення ручкою (велика тонка область довгих прямих штрихів). `New-NsOcrGray` бере в ній канал R, поза маскою піксель незмінний. Не ловить одиночну риску й коло. Перевірка хибних — `ns-penscan.py`. |
| `ns-framecheck.py`, `ns-framecompare.py`, `ns-inklost.py`, `ns-edgedepth.py`, `ns-wedge.py`, `ns-ocrprobe.py` | Дослідні заміри інструментів: рамка (біле ≥ 254) у трьох місцях; порівняння двох render; втрата темних пікселів нове проти старого; глибина скла й білого клина по краях; клин (використовує `ns-render`); проба OCR. Параметри — у скриптах і звіті `ЗВІТИ\2026-09-24_інструменти_2.md`. |
| **`ns-cropaudit.ps1 [-Year Y] [-Tol 3]`** | Огляд вад автоматичної обрізки: кілька кадрів, аркуш менший за сусідні в номері, упор у межу області. Має контрольні зразки в `removed` і без них висновку не дає. |
| **`ns-viewer.ps1`** | Живий перегляд скану на другому (вертикальному, HP X24ih) моніторі; запускається з `ns-scan` сам (`-NoView` — вимкнути). Сторінка, збільшений нижній кут із підказкою «має бути N ліворуч/праворуч», мініатюра попередньої, п'ять перевірок (`Update-NsLive` у ns-lib): розмір 4693×6583, один кадр, порожній/чорний кадр, «та сама сторінка ще раз» (RMSE < 0,10: той самий аркуш 0,015-0,031, різні 0,28-0,40), смуга кришки вздовж усього боку. Вікно не забирає фокус і займає верх екрана; нижні 380 px — консоль: `ns-menu` і `ns-scan` самі переносять туди своє вікно (`Move-NsConsoleBelowViewer`; у Windows Terminal — пошук вікна за заголовком вкладки, журнал `NS_WORK\_live\console_move.log`). Кут і мініатюру оператор назвав зайвими — лише сторінка. У `ns-scan` клавіша **R** — перезняти останню сторінку (через `ns-rescan`). |
| **`ns-pagecheck.ps1 -Year Y -Band -BandMm 20 -PerSheet 2`** | Аркуші нижніх смуг сторінок для звірки друкованого номера з номером файлу — ОЧИМА (OCR номерів відкинуто). Без `-Band` — лише кути, і на рукописному логотипі підвалу цифра в них не потрапляє. `-Seq N -SetOrder "…"` пише `page_order` (для вкладок). |
| **`ns-edgecheck.ps1 -Seq N [-Pages "1,10"] [-Brief] [-Sheet]`** | Рішення про зріз кожного краю й чому; позначені для нагляду сторони (`<< НАГЛЯД`). `-Sheet` — аркуш смуг краю з лініями 2/4/6/8 мм у `NS_WORK\edgesheet\N.png` для рішення оператора. Працює на prep; для профілю «до чистки» — prep з `-NoEdgeClean`. |
| **`ns-droppage.ps1 -Seq N -Page P -Reason "…" [-DryRun]`** | Прибрати зайву сторінку (той самий аркуш знято двічі) і підтягнути нумерацію наступних. Майстер не знищується — іде в `_catalog
emoved` з міткою часу, у маніфесті поле `dropped`. Звірка хешів до і після, суми, реєстр, журнал `_catalog\logs\droppage.log`. |
| **`ns-rotcheck.ps1 -Seq N [-Pages "6,7,8,9"]`** | Який кут потрібен сторінці: розпізнавання у 0/90/180/270 і кількість справжніх слів. Для вкладок, де OSD не певний (2323 «Світанок»: упевненість 0,5-10 при порозі 12). Каже «НЕПЕВНО», коли різниця мала — тоді дивитися очима. |
| **`ns-renumber.ps1 -Seq N -Order "…" -Reason "…" [-DryRun]`** | Перейменувати майстри, коли оператор відсканував сторінки не в тому порядку. Вміст і хеші не міняються, лише імена; звірка до й після, `renamed_from` у маніфесті, суми, реєстр, прибирає похідні. Відмовляється, якщо в маніфесті є `page_order/page_rotate/page_edge`. |
| **`ns-insert.ps1 -Seq N -Pages "3-8"`** | Позначити сторінки вкладки зі своєю нумерацією («Світанок», календар). Підказка під час сканування після цього рахує правильно: файл 15 при вкладці 3-8 має друковану 9. `-Clear` — прибрати. Те саме робить клавіша **V** у ns-scan. |
| **`ns-droppage.ps1 -Seq N -Page P -Reason "…"`** | Прибрати зайву сторінку (дубль, чорний кадр) і підтягнути нумерацію. Майстер — у `_catalog
emoved`. |
| **`ns-livecheck.ps1 -Seq N -Page P`** | Перевірки щойно відсканованої сторінки ОКРЕМИМ процесом (показ уже на екрані). Запускає ns-scan сам. |
| **`ns-rotcheck.ps1 -Seq N [-Pages "6,7"]`** | Який кут потрібен сторінці — за розпізнаванням у 0/90/180/270. Для вкладок, де OSD не певний. |
| **`ns-backup.ps1 -Dest D:\ [-VerifyOnly]`** | Резервна копія майстрів, PDF, скриптів і профілю NAPS2 + звірка за SHA-256. |
| **`python ns-inpaint.py <вхід> <маска> <вихід> [метод]`** | Заростання дірок: telea / ns / fsr / fsrbest / biharmonic. Викликається з `Repair-NsHoles`; окремо — для дослідів. |
| `ns-shortcut.ps1 [-AsAdmin]` | Створює ярлик на робочому столі. Запускати, якщо ярлик загубився. |
| `ns-migrate.ps1 [-Apply]` | Разовий перенос старої пласкої схеми в каталог. Уже відпрацював; лежить у `_старе_не_запускати\`. |

Порядок сторінок у PDF (календарні вкладки вшиті поза порядком читання)
задається `-PageOrder "1,2,3,4,8,6,7,5"` **один раз** — він записується в
маніфест номера й діє на всі подальші перезбірки. Майстри та їхні імена
ніколи не переставляються: вони лишаються в порядку сканування.

**Прав адміністратора конвеєр не потребує.** Запис іде лише в `C:\NS_MASTERS`,
`C:\NS_WORK`, `C:\NS_PDF` — усі три створені й заповнені звичайним користувачем.
Адмін був потрібен один раз, для `choco install`. Ярлик навмисно **без** UAC.

⚠︎ **`.bat`-обгорток на цій машині НЕМАЄ** (вони лишилися на старому лептопі).
Політика виконання `LocalMachine` = `Restricted`, тому `.
s-scan.ps1` напряму не
запуститься. Чинна форма — та, що в розділі «Як зі мною працювати»:
`powershell -NoProfile -ExecutionPolicy Bypass -File .
s-<назва>.ps1 <параметри>`.
Оператор працює через ярлик «Наше слово» → `ns-menu.ps1`.

⚠︎ **Текст у `.bat` — лише латиницею.** `cmd.exe` калічить кирилицю через кодову
сторінку, і покалічені рядки він намагається виконати як команди. Обгортки —
самі лише пускачі; уся взаємодія з оператором живе в `.ps1`, де кирилиця
працює нормально.

### Ще дві пастки PowerShell, на яких я вже спіткнувся

- **Імена змінних нечутливі до регістру.** Локальна `$need` усередині функції
  з параметром `$Need` — це **та сама** змінна; якщо в параметра є `ValidateSet`,
  присвоєння локальній падає з `ValidateSetFailure`. Називати локальні змінні
  інакше, ніж параметри.
- **Не покладатися на PATH у щойно запущеній оболонці.** Після встановлення
  Ghostscript його тека є в машинному PATH, але вже запущені процеси його не
  бачать. Тому Ghostscript, NAPS2 і Python у `ns-lib.ps1` шукаються за файлом,
  а не через `where`. Tesseract вписаний сталим шляхом — там, куди його
  кладе інсталятор. (До 16.09.2026 тут стверджувалося, що за файлом шукається
  й NAPS2, — насправді він був вписаний під старий лептоп. Див. нижче.)
- **Перевіряти лише потрібні інструменти.** Сканування не має падати через
  відсутній ocrmypdf — це різні етапи: `Test-NsTools -Need scan` проти `-Need ocr`.
- **Вивід PowerShell втрачає кирилицю при перенаправленні у файл.** `... > log.txt`
  віддає консольний вивід у поточній кодовій сторінці, і українські літери
  стають знаками питання **безповоротно** — у файлі вже немає чого рятувати.
  Через це лог тригодинної перезбірки довелося читати за структурою рядків,
  і двічі я прочитав його неправильно, один раз доповівши «нуль зроблено»,
  коли насправді все йшло. Лікується `Initialize-NsConsole` з `ns-lib.ps1`
  (ставить `[Console]::OutputEncoding` в UTF-8); викликається на початку
  кожного `ns-*.ps1`.
- **Допоміжні `.ps1`, згенеровані через bash, теж потребують BOM.** Та сама
  пастка, що й для основних скриптів, але про неї легко забути на разовому
  скрипті: перший запуск перезбірки впав одразу на розборі. Писати через
  Python з `encoding="utf-8-sig"`.
- **Зворотні слеші не переживають передачу рядка в Python через heredoc.**
  `'...\\ns-lib.ps1'` доїхало як `\n` — справжній перенос рядка, і заміна
  мовчки не знаходила нічого. Шукати по рядках без слешів у шаблоні або
  збирати слеш через `chr(92)`.
- **`Measure-Object` повертає `Maximum`/`Sum` як `Double`, не `Int`.** А формат
  `"{0:D2}"` існує лише для цілих типів і на `3.0` падає з «Format specifier
  was invalid». Поклало `ns-scan.ps1` рівно на відновленні перерваного номера
  (перший запуск ішов іншою гілкою, `return 1` — ціле). Результат
  `Measure-Object` завжди приводити до `[int]`/`[long]` перед форматуванням.
  Мораль ширша: **гілку відновлення треба пробувати окремо** — вона не
  перевіряється першим успішним запуском.
- **ANSI-кодова сторінка системи — 1252, кирилиці в ній немає.** Усе, що
  переганяє шлях в ANSI, ламається на `C:\Users\sever\OneDrive\Робочий стіл\…`.
  Вкусило вже двічі: `pdftoppm` мовчки не спрацював, а COM-об'єкт
  `WScript.Shell` не зміг зберегти ярлик (`????` замість назви теки). Обхід:
  робити справу в латинському шляху, а переносити засобами .NET/PowerShell.
  Третій укус знайшовся 16.09.2026: навіть у латинському шляху `WScript.Shell`
  записав **опис** ярлика знаками питання, і ніхто цього не бачив, бо помилки
  не було. Тепер `ns-shortcut.ps1` пише через `IShellLinkW` (Unicode) і
  **читає ярлик назад**, звіряючи кожне поле; розбіжність — відмова.
- **Реєстр — похідні дані, маніфест — джерело істини.** `ns-scan.ps1` оновлює
  рядок реєстру на **кожній** сторінці; якби лише в кінці, перерваний скан
  лишав би «0 сторінок». Якщо реєстр усе ж розійшовся — `Sync-NsRegistry`
  перебудовує його з маніфестів (викликається на початку `ns-verify`).

### Відкладений кадр 2240 і чого `ns-verify` не бачить

`NS_MASTERS\_catalog\removed\2240_2000-07-02_p11.tif` — чорний кадр, скан
порожнього скла. 09.09.2026 оператор велів прибрати його з номера 2240; його
відклали, а не знищили. Він **не записаний ні в маніфесті, ні в сумах** — тому
`ns-verify` рахує 736 сторінок, а TIF-файлів на диску 737. Це нормально.
Дубль сторінки 5 у 2270 видалено остаточно на прохання оператора, у `removed`
його немає.

⚠︎ **`ns-verify` не шукає файлів поза маніфестами.** `Find-NsOrphans` у
`ns-lib.ps1` є, але викликає її лише `ns-scan.ps1` для поточного номера.
Зайвий чи підкинутий TIF у теці номера перевірка не помітить. Відкрите
питання: чи додавати пошук сиріт у `ns-verify`, з винятком для `removed`.

⚠︎ Моя власна помилка на цьому місці: я звірив «737 файлів TIF» із «737
сторінок» у листі й записав збіг. Порахував файли, а назвав сторінками — і
збіг був випадковий, бо в листі теж була неточність.

**Старе покоління — ще потрібне для обробки, поки не написані `ns-prep/render/ocr/build`:**

`build_issue_pdf.ps1`, `check_batch.ps1`, `verify_issue.ps1`, `crop_masters.ps1`.

### Відновлюваність

Сторінка вважається прийнятою лише якщо виконано **всі три** умови: файл на
місці, має атрибут read-only, і його хеш збігається з маніфестом. Порядок запису:
скан у тимчасове ім'я → перевірка, що файл читається → хеш → перейменування →
read-only → атомарний дозапис у маніфест. Обрив живлення коштує щонайбільше
**однієї сторінки**. Файл у теці без запису в маніфесті вважається обірваним
записом: скрипт показує його, але **не** підхоплює мовчки.

## Що знати про поведінку

- **Сканер засинає.** Перший скан після паузи може дати «offline» або `Error 2019` —
  просто повторити.
- **Прогрів.** Перші скани сесії мають дрейф балансу білого; дати підсвітці
  10–15 хв.
- **Календарні вкладки** друкуються поперек аркуша. Їх повертають так, щоб текст
  читався, навіть якщо сторінка стає горизонтальною. Це ухвалене рішення, не
  помилка. Порядок сторінок у PDF при цьому виправляється параметром
  `-PageOrder "1,2,3,4,5,8,6,7,9,10"` — **файли на диску не перейменовуються**.
- **Автоповорот** не спрацьовує, якщо впевненість нижче 12 — такі сторінки йдуть
  до людини. Хибний поворот гірший за жоден.
- **Смуга скла.** Зазвичай ~1 мм по правому краю. `build_issue_pdf.ps1` прибирає
  її автоматично: стискає сторінку до смуги 1px по кожній з чотирьох сторін
  і шукає перехід «скло → папір». Звичайний `-trim` тут не працює — смуга буває
  лише з одного боку, і один випадковий світлий піксель на краю його зриває;
  на повернутих на 90° вкладках смуга опиняється збоку, а не згори. Вимикається
  `-NoCrop`. Майстер не чіпається, лише робоча копія.
- ⚠︎ **Запас на краях є, але невеликий і несталий.** Виміряно по майстрах:
  ширина 297,0-301,9 мм, висота 413,6-420,1 мм. Автоматична обрізка ріже по
  вмісту, тож розмір гуляє від сторінки до сторінки на кілька міліметрів.
  Захист від зрізаного краю — QC-перевірка відхилення ширини від медіани
  по річнику (>3 %), вона працює.
- **Корекція кольору й дескринування** живуть у `build_issue_pdf.ps1`, не в
  сканері: `-modulate 100,150,102` (підводить синій до ~1° відтінку від зразка
  1999 р., нейтральний чорний текст не зачіпає — перевірено попіксельно) і
  `-gaussian-blur 0x1.0 -unsharp 0x1.5+1.2+0.02` (прибирає розтрові крапки,
  знижує помилки OCR з 2,3 % до 1,5 %). Радіус 2,2 пробувався й відкинутий —
  прибирає крапки краще, але помітно розмиває текст.
- **Вирівнювання нахилу** — теж у `build_issue_pdf.ps1` (`-deskew 40%`), не
  в профілі NAPS2. Вимикається `-NoDeskew`.
- **Сторінки виходять різного розміру**, тому `build_issue_pdf.ps1` доповнює
  кожну білим полем по центру до спільного розміру в межах випуску — портретні
  й горизонтальні вкладки окремими групами. Вимикається `-NoPad`.
- **NAPS2 перезаписує частину налаштувань драйвера.** У режимі «Use predefined
  settings» він сам передає Page size/Width/Length на кожному скані, затираючи
  те, що виставлено в самому драйвері. А Color Matching тримається, бо такого
  поля в діалозі NAPS2 просто немає. Якщо якесь налаштування драйвера «не
  тримається» — спершу перевірити, чи є для нього поле в профілі NAPS2.

## Остаточний стандарт видачі (18.09.2026) — ЧИННИЙ

Погоджено оператором (16-18.09.2026). Усе, що в розділах нижче суперечить цьому
списку, — історія.

1. **Брудний край ВІДРІЗАЄТЬСЯ, не замальовується.** Вигляд словами оператора:
   «текст, далі газетний папір і далі рамка» — без дірок, смуг, сходинок.
   Глибину визначає `Get-NsEdgeCut` (ns-lib) за профілем трьох мір по глибині
   (частка фарби, частка темної фарби, середня яскравість):
   - **верх/низ — лише темний край** (смуга скла, лінія кришки, тінь обрізу),
     зазвичай 1-2,5 мм; чисте поле лишається (7 мм зрізаного низу оператор
     назвав «помітно і неохайно»); темна плашка до краю — не ріжеться;
   - **боки — 8 мм** при проколах, смузі скла, фото чи фоні на виліт («на боках
     різати сміливо, важливої інформації там немає»); **не ближче 1,5 мм** до
     друку, перед яким є чиста смуга паперу (2266/10 лів., 2254/2 лів.);
   - вказівка оператора в маніфесті `page_edge` має перевагу над усім і
     вимикає на тому краї й детектор смуги скла.
   - **21.09.2026 — місцевий запобіжник і нагляд оператора.** На 2294/1
     (логотип справа, літери банера «Лемківська ватра» зліва), 2308/1 (логотип)
     і 2308/10 лів. (текст з 5,5 мм) 8 мм різали друк: частка по ВСІЙ довжині
     краю не бачить логотипа чи кількох рядків (знову «усереднено локальне»).
     Тепер `Get-NsEdgeLocal` шукає друк у кожному відрізку окремо; сторона, де
     друк ближче за зріз або зріз іде по вильоту, позначається **Review**, і
     до вказівки оператора на ній ріжеться ЛИШЕ темний край (0,5-3,5 мм).
     Глибше — лише `page_edge` після аркуша `ns-edgecheck.ps1 -Seq N -Sheet`.
     Калібрування (prep з `-NoEdgeClean`!): 2290 — 0 позначок; 2266/5, 6
     (дірки + смуга) — 8 мм, як велів оператор; 2266 — 6 сторінок з 10 (фото
     на виліт), 2294 — 5, 2308 — 3. Детектор НЕ розрізняє надійно фото на виліт
     і логотип біля краю — тому безпеку дає нагляд, а не він.
     ⚠︎ **Того ж вечора скасовано** «лише темний край» на позначених сторонах:
     дірки на фото лишалися (2294/5, 10), а ширші на 10-15 мм сторінки роздували
     спільний розмір — рамка на решті ставала 9-12 мм (оператор: «рамка
     нерівна»). Виліт знову ріжеться 8 мм; захищається лише друк після чистого
     паперу. Сторінки, які через захищений друк ширші (2294/1 — логотип під
     край), `ns-render -FitScale` стискає ДО МЕДІАНИ номера (не розтягує решту;
     перша версія ключа розтягувала 9 сторінок на 1,5-3,9 %): 2294/1 −3,75 %,
     2308/1 −1,8 %, /10 −1,9 %; рамка на всіх 6,7-7,1 мм. Межа 4 % — на
     погодження оператору.
     ⚠︎ Діагностику профілю краю робити лише на prep з `-NoEdgeClean`: звичайний
     prep уже залив смугу тоном паперу, і профіль бачить порожні 8 мм.
2. **Усі сторінки номера одного розміру** (книжкові окремо, альбомні окремо):
   лишок зрізається з чистого поля, яке `ns-prep` виміряв до друку (fT..fR у
   `_edge.txt`), пропорційно запасу; друк заради формату не ріжеться — сторінка,
   якої не звести, доповнюється рамкою (журнал `ns-render` це пише).
   **З 25.09.2026 (рішення оператора) — СТАНДАРТ:** `ns-render` знімає білі
   клини по краях і розтягує вужчу сторінку до 1,5 % (`-FitGrowMaxPct 1.5`;
   стиснення — до 4 %; `-NoWedge` вимикає клин). A/B на 4 номерах: нерівних
   рамок 30 → 5, втрачено 0 темних пікселів. Попереднє «не розтягувати» —
   історія. 2267, 2318, 2319 перебудовуються з новою рамкою.
3. **Рамка БІЛА 255, 7 мм** навколо спільного розміру (`FrameMm=7`: 8 мм оператор
   назвав грубою, 7 — як у PDF 2225).
4. **Баланс білого — один на номер**, папір → 244. **Вирівнювання паперу між
   сторінками НЕ робиться** — різниця аркушів справжня («якомога правдивіше
   передати скан»). Насиченість і зсув відтінку вимкнено (`-HueFix`).
5. **OCR:** сірий переклад, Sauvola (`thresholding_method=2`), ukr+pol; **плашки
   зі світлим текстом на темному інвертуються в сірій копії** (`New-NsOcrGray`,
   з 18.09.2026: на 2266/4 з 2 до 8 контрольних слів із 8; фото не чіпаються —
   плашкою вважається темна область, що заливає >= 85 % свого габариту).
6. **Календарні вкладки** — поворот, свій порядок у PDF, свої глибини країв у
   маніфесті (2214, 2267).
7. Після збірки — `ns-qc` і `python ns-edgescan.py`; позначене дивитися очима.

⚠︎ **Перед перезбіркою 2000 року** переглянути `ns-rebuild-frame-2000.ps1`: там
примусові 7 мм ЗГОРИ (2215 4T7 9T7, 2219 8T7, 2220 1T7, 2223 7T7, 2227 1T7 3T7
6T7, 2228 2T7, 2232 3T7 4T7) — суперечать п. 1 (верх — лише темний край).
Ручні вказівки 2214 (календар, марля L14.5), 2237 (2R12.5), 2254 зберегти.
Обережно з 2235 стор. 13 (колонтитул «СВІТАНОК 8» за 3-8 мм від краю).

## Стан проєкту і особливості номерів — зі слів старої сесії (18.09.2026)

Перевірено нею на старому лептопі, не з пам'яті.

- **Старий лептоп більше не сканує й не збирає.** Останній скан там —
  15.09 11:58. Його каталог тепер ВІДСТАЄ: там досі розрізані 2266/8, 2267/4,
  старі 2268/10 і 2280, немає 2286. Джерело правди — нова машина.
- ⚠︎ **Резервної копії актуального каталогу НЕМАЄ.** Незалежної повної копії не
  було ніколи. Флешка переїзду (16.09) не має замін 17.09 і 2286. У оператора є
  друга порожня флешка 64 ГБ — 39 ГБ влізуть.
- **План** `optimized-rolling-ocean.md` лежить лише на старому лептопі
  (`C:\Users\sever\.claude\plans\`). Фази 1-4 зроблено; фаза 5 — замість річника
  2007 пройдено весь 2000. У коді ще немає: HTML-звіту на річник і щоденної
  синхронізації на другий диск (фаза 6). Пересканування 2214-2218 з плану —
  НЕ недоробка: 07.09 оператор вирішив «перескановувати 2214 і 2215 не буду, там
  все добре» (номери 2000 р. уже перезнято після розшиття з чорним тлом; сталий
  A3 у 2214/2215 — наслідок тодішнього профілю, не вада). Застаріле в плані:
  200 dpi, область 295×415, окремі ns-ocr і ns-new-issue.
- **Архів проєкту** — на флешці переїзду `D:\АРХІВ_ПРОЄКТУ` (склала стара сесія
  18.09): хронологія 11.08-18.09 (PDF/HTML, для керівництва — доповнювати при
  нових рішеннях), `План\optimized-rolling-ocean.md`, `Зразки\` (173 МБ),
  `Розмови\` (375 МБ транскриптів JSONL — лише grep-ом, у контекст не вантажити).
  NS_MASTERS на тій флешці — копія від 16.09, без замін 17.09 і без 2286; якщо
  робити з неї резерв — ОНОВЛЮВАТИ, і `robocopy /MIR` лише з відома оператора.
- **Походження ручних `page_edge` у маніфестах 2000 року:**
  - 2254 `1L3 5L3 7L3 9L3 11L3 8R3 10R3` — рішення ОПЕРАТОРА;
  - 2227 `1T7 3T7 6T7 4R7 6R7 8R7` — НЕ оператора, виміряний список старої
    сесії з 16.09; три перші (7 мм згори) суперечать стандарту — переобчислити
    за `Get-NsEdgeCut` і погодити з оператором;
  - 2237 `2R12.5` — виміряний (смуга + проколи до 11 мм, друк з 25,6);
  - 2214 — нулі календаря й порядок від оператора, `1L14.5`, `7B11 8B11` — виміри.
  Решта 32 країв (2215-2245) живе лише в `ns-rebuild-frame-2000.ps1`.
- **Особливості номерів:**
  - 2214, 2215 — старий A3-профіль (усі сторінки 4677×6615); 2214-2216, за
    планом, деструктивно обрізані на місці `crop_masters.ps1` (рішення оператора);
  - 2225 — номер для показу керівництву («Цифрове "Наше слово"»);
  - 2235 — сторінки 9-14 повернуті OSD автоматично, `page_rotate` у маніфесті
    НЕМАЄ (якщо OSD колись поведеться інакше — записати вручну);
  - 2239/6, 2244/5 — шпальти фото, мало слів; 2244/5 — фото заходять у поля;
  - 2248 — стор. 8 і 9 були переплутані, виправлено 09.09 ПЕРЕЙМЕНУВАННЯМ
    майстрів; слідів у маніфесті немає, `page_order` не потрібен;
  - 2259 — «Світанок», `page_rotate 9:90,11:90,13:90`;
  - 2294, 2314 — сторінки відскановано не в тому порядку (2294: стор. 3 лежала
    в p10; 2314: p04/p05 навпаки). 21.09.2026 майстри ПЕРЕЙМЕНОВАНО
    `ns-renumber.ps1` за рішенням оператора («я помилився — виправ імена»),
    сліди — `renamed_from` у маніфесті і `_catalog\logs\renumber.log`.
    Звірка підвалів усього 2001 року (510 сторінок) інших розбіжностей не знайшла;
    2289 стор. 10 — факсиміле історичної першої сторінки, без підвалу;
  - **2002 рік — великі проколи на корінці** (оператор, 21.09.2026). Виміряно
    на 2319: ряд дрібних проколів (1,8-2,8 мм) на 0-3 мм і по два ВЕЛИКІ
    (7,6 x 6,5 мм) на 2,9-10,5 мм углиб; текст біля корінця — з 18-20 мм.
    Спершу різали корінець на 13 мм (`NS_SPINE_RULES`), але поля виходили
    несиметричні на 2,5-7,7 мм, і оператор це побачив.
    **ЧИННЕ — рішення оператора 24.09.2026:** спроби заростити всі проколи
    невдалі; **дрібні дірочки від нитки ЗРІЗАЮТЬСЯ, дві великі дірки від
    зшивача ЗАРОСТАЮТЬ.** Пробується спершу на 2318 (відділ інструментів),
    на збирання — після схвалення оператором. Далі в цьому пункті — історія.
    **Рішення оператора 23.09.2026 — варіант A: дірки ЛАТАЮТЬСЯ, зріз 8 мм
    як на звичайному боці.** `Repair-NsHoles` (ns-lib) бере латку з тієї ж
    смуги краю, зсунутої вздовж краю (12, -7, 19, -23 мм — кілька проходів,
    бо під першою латкою може бути інша дірка), тож зберігається зерно й тон
    паперу. Це РЕТУШ: у копії для читання з'являється папір там, де в аркуші
    дірка. Майстер недоторканий, рішення свідоме.
    ⚠︎ **23.09.2026, те саме латання зіпсувало 2318.** На 2319 смуга краю —
    чистий папір, і копіювання виглядало бездоганно. На 2318 у тій смузі стоїть
    друк, і латка ПЕРЕНЕСЛА його: у календарі зникла «С» у «Січні» та «К» у
    «Квітні», з'явилися зайві цифри, біля ікони на стор. 9 подвоївся візерунок,
    у підвалах — сміття. Обробка змінила ЗМІСТ сторінки, і побачив це оператор,
    не я: мої перевірки порівнюють номер сам із собою й такого не ловлять.
    **Копіювання скасовано.** Тепер `Repair-NsHoles` заливає пляму ТОНОМ ПАПЕРУ
    сторінки із зерном і лише тоді, коли навколо плями (кільце ~4 мм) чистий
    папір; біля друку, рамки чи календарної сітки дірка лишається. На 2318 так
    заповнилася одна пляма з усіх — і це правильна відповідь.
    Мораль ширша за цей випадок: **ретуш, відкалібрована на чистому полі, на
    іншому номері працює по друку.** Будь-яку заміну пікселів пробувати на
    номері з версткою до краю, а не лише там, де вона виглядає добре.
    Дві вади, на яких це стояло (23.09.2026):
    ⚠︎ маска з альфа-каналом (непрозорим) змушувала `-composite` копіювати ВСЮ
    смугу — латання лише зсувало зображення, а плями лишалися на місці, і
    журнал бадьоро писав «залатано 72». Тепер `-alpha off` на масці;
    ⚠︎ межа розміру плями 7 мм відкидала саме великі проколи (7,6 мм) — тепер
    9 мм; межу дав ВИМІР плям, а не око.
    Якщо в наступному річнику інше зшивання — поміряти так само (prep
    `-NoEdgeClean` + `ns-edgecheck -Sheet`) і додати рядок у `NS_SPINE_RULES`;
  - **2331 — замість 5-ї сторінки вдруге знято 3-тю** (оператор, 23.09.2026).
    Прибрано `ns-droppage.ps1`, наступні сторінки підтягнуто, дубль лежить у
    `_catalog
emoved`. ⚠︎ **Перевірка «та сама сторінка ще раз» цього не бачила
    в принципі**: вона порівнювала новий скан ЛИШЕ з попередньою сторінкою, а
    сусідні сторінки завжди різні. Тепер `Update-NsLive` звіряє кожен скан з
    УСІМА вже знятими сторінками номера (малі копії кешуються в `NS_WORK\_live`);
    перевірено на самому дублі — 0,031 проти стор. 3, справжня 5-та чиста.
    Ще один випадок «вимір не того місця»: міряли пару, а помилка була в наборі.
  - **2318 — збирається за посторінковими вказівками оператора** (23.09.2026):
    `page_edge "1T3 1L0 3L3.5 4L0 5L3.5 5R0 6L0 6R0 7L3.5 7R0 8L0 8R3.5 9L3.5
    9R0 10T6 10L0 10R3.5"`. Суть: на корінці різати лише дрібні проколи (3,5 мм),
    решту країв не чіпати; на календарних сторінках 5-6 не різати зовсім (зріз
    забирає числа календаря); великі проколи лишаються, бо навколо них друк.
    Чорна смуга згори стор. 1 (3 мм) і стор. 10 (6 мм) — зрізана; чорний
    трикутник у куті стор. 10 — це кришка, видима крізь ОБІРВАНИЙ КУТ, зрізом
    не лікується.
  - **2323 — вкладка «Світанок», стор. 6-10 надруковані впоперек.** Кути в
    маніфесті: `page_rotate 6:270,7:90,8:270,9:90,10:270`. OSD був невпевнений
    (0,5-10 при порозі 12) і сторінки лишалися боком; кут визначено
    `ns-rotcheck.ps1` (розпізнавання в чотирьох поворотах) і перевірено очима.
    На повернутих сторінках бік корінця перераховується за кутом (Get-NsSpineRule).
  - **2318 — обірвані кутики аркушів** (оператор, 21.09.2026). При збиранні
    бути уважним: у вирваному куті видно чорну кришку, і обрізка смуги скла чи
    зріз краю можуть поставитися до нього як до смуги або виліту; переглянути
    краї (`ns-edgecheck -Sheet`) і готовий PDF очима;
  - **2317 фізично немає в річнику** (оператор, 21.09.2026): 2002 рік
    починається з 2318. Записано в `_catalog\missing.csv` (дата 2001-12-30 і
    № 52 — розрахункові); меню його пропускає, перевірка розривів не скаржиться;
  - правило: помилка оператора в порядку → перейменувати майстри; вкладка,
    вшита поза порядком читання → `page_order`, майстри не чіпати;
  - сходинка паперу між аркушами (не вирівнювати): 2218, 2227, 2239, 2249,
    2254, 2259, 2265.
- **`.bat`-обгортки** (`ns-scan.bat`, `ns-verify.bat`) є лише на старому лептопі;
  на новій машині не потрібні — оператор працює через ярлик і меню, а Claude
  запускає `powershell -File`. `scan_issue.bat` — прототип із
  Brightness -50/Contrast -100, НЕ переносити.
- **Доставка в Об'єднання:** нічого ще не передавали. Відомо: ім'я файлу =
  наскрізний номер, 300 dpi / q55, PDF/A-2b, метадані з маніфесту. Звідки
  формула теки «Nasze Słowo - rocznik <рік>» — спитати оператора.
- **Що варто забрати зі старого лептопа:** `C:\NS_WORK\_zrazky` (173 МБ, зразки
  рішень оператора про рамку, баланс, обрізку). Еталонні номери архіву
  (2183, 2199, 2213…) — в OneDrive\ND-PROJECTS\Nasze Slowo.
- **Принцип оператора:** «якомога правдивіше передати дані якісного сканування,
  щоб не виникло спотворення сканів обробками».

## Відкриті питання

- **Зовнішній диск.** На C: вільно ~150 ГБ (вистачає на ~220 номерів). Весь
  проєкт — ~485 ГБ майстрів плюс стільки ж на резерв. Шляхи в нових скриптах
  робляться налаштовуваними, щоб переїзд був зміною одного значення.
- **Резервне копіювання** після кожного випуску + `verify_issue.ps1`.
- **Якість OCR: ~1 % слів псується, і це свідома ціна.** У парі `ukr+pol`
  Tesseract іноді бере латинського двійника замість кириличної літери:
  `XPOHIKA` замість `ХРОНІКА`, `icTOpHKa` замість `історика`, `MOBOIO` замість
  `мовою`. Заміряно на реальній сторінці:

  | Мова | Слів | Зіпсованих |
  |---|---|---|
  | `ukr` сам | 2322 | **0 (0,00 %)** |
  | `ukr+pol` | 2248 | 21 (0,93 %) |

  **Але `ukr` сам знищує польський текст повністю** (`W dniach 5-8 grudnia` →
  `М апіасн 5-8 дгодпіа`), а газета містить польські тексти регулярно. Тому
  `ukr+pol` лишається: 1 % зіпсованих кириличних слів — менша шкода, ніж
  нечитабельна польська. Помилки лише в **текстовому шарі**; саме зображення
  завжди правильне, тож архівний запис не страждає — лише пошук іноді не знайде
  слова.

  **Автоматична метрика замість ручної звірки.** Цей клас помилок ловиться
  програмно, без читання очима: слово, у якому змішані кирилиця й латиниця, або
  слово повністю з латинських двійників (`ABCEHIKMOPTXYaceiopxy`) довше 2 літер —
  майже завжди помилка. На тесті: 25 знайдених, **усі 25 справжні помилки, жодного
  хибного спрацювання**. Це і є показник якості OCR для QC (норма ~1 %, різке
  зростання = проблема зі сканом).

  **Порожня сторінка ловиться порівнянням із сусідами, не абсолютним числом.**
  Газетна сторінка має 1300–2200 слів, але календарна вкладка чи сторінка
  самих фотографій законно мають десятки. `ns-qc.ps1` позначає сторінку, де
  слів менше за 15 % медіани по номеру. На 19 номерах спрацювало двічі, обидва
  рази на справжньому вмісті: **2214 стор. 5–8** — календарна вкладка (сітка
  дає високу періодичність в обох напрямках, тому це не повернутий текст);
  **2231 стор. 1** — великодній номер, привітання каліграфією плюс вірш
  Антонича замість новин. Це не помилка, а привід глянути — і огляд дешевий.

  ⚠︎ Поріг на цілий номер такого не ловить: 2214 має 7154 слова проти ~14 000
  у сусідів і проходить будь-який розумний загальний поріг.

  Підтверджено на цілому номері 2231: **12 370 слів, 111 підозрілих — 0,90 %**,
  тобто рівно та сама величина, що й на окремій сторінці. Поріг у `ns-qc.ps1`
  виставлено на 2 %: він ловить зіпсований скан, а не звичайний фон помилок.
  Приклади з 2231: `BOCKPEC`, `MACOB`, `AKTHBHY`, `Mapia`, `POKY`.
- **Правове.** Номери 2000–2013 — твори живих авторів у повній охороні.
  Оцифрування для внутрішнього архіву цього не потребує, але публікація на сайт
  потребує ревізії авторських договорів редакції. Питання до Об'єднання, не до
  коду, але має бути вирішене **до** публікації.

## Головна пастка проєкту: вимір не того місця

Це повторилося **п'ятнадцять разів** (п'ять — за один день), щоразу в новому вигляді. Жодного
разу не йшлося про помилку обчислення — число завжди рахувалося правильно, але
не про те, що треба.

| Що міряли | Чому не те | Наслідок |
|---|---|---|
| відступ до тексту, ігноруючи зовнішні 8 мм | сліпота саме в тій зоні, яку захищаємо | стерто текст на краю |
| середнє по всій довжині краю | кутовий підвал губиться в середньому | стерто дату |
| ширину сторінки проти спільної медіани | альбомні вкладки міряні книжковою міркою | «відхилення 40 %» на справному вмісті |
| відтінок паперу з 1-ї сторінки | там шапка з великим синім логотипом | хибні зауваження на сусідніх номерах |
| яскравість смуги ВІД краю до глибини d | темний край краю домінує на всіх глибинах | «текст скрізь» замість профілю |
| профіль краю через `-crop 177x100%` | ImageMagick читає обидва розміри як відсотки, тобто бере всю ширину сторінки | «марля 9–22 мм» замість справжніх 3–16,6 мм |
| довжина темної плями вздовж краю (щоб відрізнити проколи від фото навиліт) | смуга скла тягнеться на всю сторону так само, як фото навиліт | класифікатор назвав «вмістом» ту саму смугу 2237, яку я перед тим бачив на зображенні |
| середня яскравість смуги 3 мм від краю | чорна лінія 1 мм, усереднена з 2 мм паперу, дає 160 при порозі 100 | детектор оголосив увесь рік чистим, не побачивши смуги, яку я щойно розглядав |
| кількість слів у PDF через `pdftotext` | poppler не читає GlyphLessFont повністю | «зник текст заголовків», потім «зникло три чверті тексту» — насправді все на місці |
| початок друку, шуканий від сталих 2 мм від краю | на тій глибині ще триває темрява обрізу і рядок проколів | 8 країв дістали вирок «друк за 2,0 мм» і лишилися брудними |
| висновок про чужу обробку з одного підсумкового числа | розкид 2-5 однаково пояснюється і рівним папером, і вирівнюванням | сказав оператору, що архів вирівнює сторінки — доказів немає |
| жорсткі шляхи пошуком `Program Files` і з `grep -v '^ns-rebuild'` | шлях NAPS2 вів в `AppData`, а виключений файл якраз мав жорсткі шляхи | запевнив «більше ніде немає» — на новій машині знайшлося ще три місця |
| наявність Python як «команда `python` існує» | на чистій Windows це ярлик магазину, який нічого не запускає | скрипт встановлення пропустив би Python і всі пакунки OCR |
| цілість завантаженого інсталятора за заголовком `MZ` | заголовок є і в обрізаному файлі; розбіжність 40 МБ проти ~80 пояснили, а не перевірили | обірваний інсталятор Ghostscript пролежав на флешці як «перевірений» |
| дублікат сторінки — порівнянням лише з ПОПЕРЕДНЬОЮ сторінкою | сусідні сторінки завжди різні; повтор буває з будь-якою раніше знятою | 2331: замість 5-ї вдруге знято 3-тю, перевірка мовчала, побачив оператор |
| кількість сторінок як кількість TIF-файлів | у `_catalog\removed` лежить відкладений кадр, що не є сторінкою | «737 = 737» — випадковий збіг двох неточностей |
| «скан вдався» як «`identify` повернув непорожній розмір» | на TIF із двома кадрами він повертає злиплі розміри `567x1904692x5811` — теж непорожні | 2266 стор. 8 і 2267 стор. 4 розрізані обрізкою «Automatic Multiple», пройшли `ns-scan` і `ns-verify` (хеші збігалися); знайшлося лише на збірці 16.09.2026. Тепер `Get-NsFrameCount` у `ns-scan`, `ns-verify`, `ns-qc` |

⚠︎ **Правило: перш ніж вірити числу, спитати, що саме воно міряє.** Конкретно:
- чи не виключена з виміру та сама зона, про яку йдеться;
- чи не усереднюється те, що за природою локальне (кут, вкладка, одна шпальта);
- чи порівнюються однорідні речі (книжкове з книжковим, середина з серединою);
- чи не домінує в замірі щось стороннє (край аркуша, логотип, рамка).

І друге, не менш важливе: **усі перевірки цього проєкту порівнюють номер сам
із собою або з сусідами.** Тому вада, яка псує всі сторінки однаково, для них
невидима — порівнювати немає з чим. Обидві найгірші вади дня (зрізана на 55 мм
сторінка і стерта дата) знайшов оператор очима, а не перевірки. Перегляд
готового номера на великому екрані — не формальність, а єдиний спосіб спіймати
цей клас помилок.

## Як зі мною працювати

- **Сесія сама стежить за своєю вартістю** (правило оператора, 24.09.2026).
  Довга сесія дорога: кожна відповідь несе всю попередню розмову, а один
  перегляд зображення коштує 1-2,5 тис. токенів. Тому:
  - **перед дорогою дією** — переглядом багатьох зображень, довгою серією
    перезбірок — сказати одним рядком, що це коштуватиме, і запропонувати
    дешевший шлях (числа замість картинок, вибірка замість усього року);
  - **коли задачу закрито** і оператор переходить до іншої теми — одним рядком
    запропонувати відкрити нову сесію того самого відділу: «тут уже довга
    розмова, наступне краще почати з чистої сесії»;
  - **ознаки, що пора**: перейдено до нової теми; переглянуто десятки
    зображень; іде довга розробка з багатьма пробами;
  - **не нагадувати частіше ніж раз на тему** — попередження, що повторюється,
    дратує і саме коштує токенів;
  - пам'ять проєкту у файлах (`СТАН.md`, `ЗАВДАННЯ.md`, `ЗВІТИ\`), тому нова
    сесія нічого не втрачає — це головна причина, чому обнулятися дешево.
- **Нічого не сканувати без відома оператора** — сканер фізичний, він поруч.
- **Показувати текстові звіти, не зображення.** Кожен перегляд сторінки коштує
  тисячі токенів. Дивитися на сторінку лише тоді, коли її позначила перевірка.
- Рутина мене не потребує: команди працюють самостійно.
- **Проєктні скрипти Claude запускає однією сталою формою**, з теки `Scans`:
  `powershell -NoProfile -ExecutionPolicy Bypass -File .\ns-<назва>.ps1 <параметри>`.
  Без `Start-Process`, без шляхів зі змінних, без `$env:Path = ...` у команді —
  так оператор бачить, що саме запускається, а дозволи лягають одним правилом.
  Довгі прогони — фоном, вивід збирає сам інструмент.
- **Не генерувати тимчасових скриптів для повторюваних перевірок.** Якщо
  перевірку доведеться робити ще раз — оформити її `ns-*.ps1` у `Scans`
  (як `ns-compare.ps1`) і викликати одним рядком.
- Після встановлення програм PATH оновлюється **перезапуском застосунку**
  (перезапускає оператор), а не латками в командах.
